{
    # Global options
    email your-email@example.com
}

# Replace with your actual domain
# For wildcard SSL, you usually need a DNS plugin (e.g. caddy-dns/cloudflare)
*.yourdomain.com {
    reverse_proxy app:3000
}

# Fallback for the main domain
yourdomain.com {
    reverse_proxy app:3000
}
